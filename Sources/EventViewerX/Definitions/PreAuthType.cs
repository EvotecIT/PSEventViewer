namespace EventViewerX;

/// <summary>
/// Represents Kerberos pre-authentication types.
/// </summary>
/// <remarks>
/// Events currently expose pre-authentication as a numeric string; when mapped, values such as <c>2</c> (PASSWORD),
/// <c>15</c> (PKINIT), and <c>19</c> (FASTER) can be added here for strong typing.
/// </remarks>
public enum PreAuthType {
    // Add appropriate values here
}
